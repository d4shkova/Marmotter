[package]
name = "marmotter-transport"
description = "TCP and TLS sockets for Marmotter. Moves bytes; does not parse IRC."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints.rust]
unsafe_code = "forbid"

[dependencies]
